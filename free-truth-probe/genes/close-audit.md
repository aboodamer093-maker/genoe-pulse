# GENOE FORTRESS — Systematics-Close Audit (run-3 → run-4 purge trigger)

Measured at origin/main tip; medicine banked; honest ledger, no hand-waving.

## The wart (diagnosed, not dreamed)
The persist step of this pipeline used to run `git restore --staged $DEL` for **every**
staged deletion. Consequence: any run-owned receipt that a lane honestly failed to
regenerate was **resurrected** instead of being deleted. Two such legacy cells were
proven to keep coming back run after run:

| Legacy cell | `at` (last honest seal) | Live lane in today's orchestrator? |
|---|---|---|
| `free-truth-probe/genes/receipts/vein-h-safari.json` | 2026-09-13T15:50:59.904Z | **No** (`live=False`) |
| `free-truth-probe/genes/receipts/engine-wincairo.json` | 2026-09-13T00:41:28.603Z | **No** (`live=False`) |

Neither cell has a live lane in the current pulse-orchestrator (verified: `live=False`
for both). They are **stale legacy** — their honest fate is permanent deletion, not
resurrection.

## The medicine (banked at origin/main)
The persist step **on origin/main is now `protect-chain-only`**:

- protects from deletion **only the sealed chain**: `beats/`, `genesis.json`,
  `index.json`, `chain-verify.json` (verified `True` at tip);
- **no longer resurrects** run-owned receipts that this run failed to regenerate
  (verified: the `restore-everything` wart is gone from tip).

Consequence for the next pulse (run 4): the two legacy cells above are run-owned,
were **not** regenerated this run (no live lane), and thus will be **deleted for real**
by the honest persist — leaving **zero stale cells** in the tree.

## Honest verdict at this audit
- persist = protect-chain-only: **True** (medicine is banked)
- `vein-h-safari` live-in-orchestrator: **False** → legacy → must die
- `engine-wincairo` live-in-orchestrator: **False** → legacy → must die
- All other receipts are FRESH (at=20:0x this burst) — honest, no silent cells.

## Invariant this document testifies
Zero silent cells. Zero stale cells. A run-owned receipt that is not regenerated by
a live lane is deleted and **never resurrected**. `FORTRESS` remains standing because
the medicine we banked is now in the workflow the next pulse executes.
